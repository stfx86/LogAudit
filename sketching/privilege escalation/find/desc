command :	find / -perm -u=s -type f 

This is a SUID binary enumeration command — it searches the entire filesystem for executables with the Set-UID bit set (files that run as their owner, typically root, regardless of who executes them).
